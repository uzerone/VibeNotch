# Swift Package Manager
.build/
Package.resolved
.swiftpm/

# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Logs / temp
*.log
/tmp/

# Claude Code local state
/.claude/

# DynamicNotch fork — kept locally as a design/motion reference only, not shipped
/study/

# Build artifacts — users compile their own .dmg via scripts/build-dmg.sh
*.dmg

# Icon Composer working files (the compiled icon/AppIcon.icns is what ships)
/VibeNotch.icon/
/VibeNotch Exports/
/icon/AppIcon.iconset/
